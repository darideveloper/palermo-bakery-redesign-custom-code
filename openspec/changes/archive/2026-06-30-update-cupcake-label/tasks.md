## 1. Update label in JavaScript

- [x] 1.1 Change visible toothpick flag text from "Ask Me" to "Contact Us" in `src/features/popup-form/custom-popup-form.js`
- [x] 1.2 Update `aria-label` on trigger button from "Ask Me" to "Contact Us" in same file
- [x] 1.3 Update `alt` text on cupcake image from "Ask Me" to "Contact Us" in same file

## 2. Verify

- [x] 2.1 Confirm cupcake button renders with "Contact Us" label in browser
- [x] 2.2 Confirm `aria-label` and `alt` text are updated (inspect DOM)
