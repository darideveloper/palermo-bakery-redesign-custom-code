## 1. Style Implementation

- [x] 1.1 In `custom-auth.css`, define the `.tml` class to target the Theme My Login container
- [x] 1.2 Apply `max-width: 600px` to constrain the form width on desktop
- [x] 1.3 Apply `margin: 50px auto !important` to center the form horizontally and provide vertical padding

## 2. Verification

- [x] 2.1 Verify that the `/login` page displays a centered, 600px wide form on desktop viewports
- [x] 2.2 Verify that the `/register` page displays a centered, 600px wide form on desktop viewports
- [x] 2.3 Confirm that on mobile viewports, the form expands naturally to fit the screen while maintaining vertical margins
