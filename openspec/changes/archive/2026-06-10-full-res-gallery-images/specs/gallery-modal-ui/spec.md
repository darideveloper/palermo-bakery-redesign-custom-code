# gallery-modal-ui Specification (Delta)

## Purpose
This delta is a marker only. The gallery-modal-ui capability has no requirement changes — the lightbox button sizing, tooltips, and hover states remain identical. All URL-related changes (full-res grid images, lightbox link generation) are covered by the gallery-optimization delta spec.

## ADDED Requirements

*(No new requirements — this capability is unchanged by this change)*

## MODIFIED Requirements

*(No requirements modified — this capability is unchanged by this change)*

## REMOVED Requirements

*(No requirements removed — this capability is unchanged by this change)*
