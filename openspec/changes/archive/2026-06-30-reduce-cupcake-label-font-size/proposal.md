## Why

The toothpick flag on the cupcake button reads "Contact Us" at 12px — visually slightly oversized for the small flag element. Reducing to 10px makes it fit better within the flag boundaries and improves overall polish.

## What Changes

- Change `.toothpick-flag` font-size from `12px` to `10px` in `custom-popup-form.css`

## Capabilities

### New Capabilities
*(none)*

### Modified Capabilities
*(none — visual styling detail, not a spec-level requirement change)*

## Impact

- `src/features/popup-form/custom-popup-form.css` — single CSS property change
